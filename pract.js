const button = document.getElementById("hi")
let img = document.getElementsByClassName("image-left")
console.log(img)
button.onclick= changeimage

function changeimage(file)
{
    let img = document.querySelectorAll(".image-left")
    
}
